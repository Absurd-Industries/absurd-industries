---
/**
 * Steam-style maker profile detail page.
 * Generated at build time from the makers data via getStaticPaths().
 *
 * Layout: profile banner, two-column body (main + sidebar), with
 * showcase, activity feed, projects grid, workshop section,
 * comment wall, badges, skills, and about panels.
 */

import BaseLayout from "../../layouts/BaseLayout.astro";
import Card from "../../components/Card.astro";
import Tag from "../../components/Tag.astro";
import ProgressBar from "../../components/ProgressBar.astro";
import { makers } from "../../data/makers";
import type { Maker } from "../../types";

export function getStaticPaths() {
  return makers.map((maker) => ({
    params: { slug: maker.slug },
    props: { maker },
  }));
}

const { maker } = Astro.props as { maker: Maker };

// ────────────────────────────────────────────────────────────
// Per-maker enriched data (mock / derived for static build)
// In production these would come from the API.
// ────────────────────────────────────────────────────────────

/** Banner colour — use avatarColor as the default */
const bannerColor = maker.avatarColor;
const bannerAccent = "#FF5900";

const initial = maker.name.charAt(0).toUpperCase();
const firstName = maker.name.split(" ")[0];

/** Mock level based on projects + backers */
const level = Math.min(Math.floor((maker.projects * 2 + maker.backers / 20)), 30);

/** Showcase — first project (mock campaign data) */
const showcase = {
  name: `${firstName}'s Featured Project`,
  description: maker.bio ?? `Open-source ${maker.specialty.toLowerCase()} project by ${maker.name}.`,
  icon: getSpecialtyIcon(maker.specialty),
  category: maker.specialty,
  license: "CERN-OHL-S",
  percent: Math.min(Math.round((maker.backers / (maker.backers + 50)) * 100), 100),
  goal: `₹${Math.round((maker.backers * 1400) / 100000).toFixed(0)},00,000`,
  backers: maker.backers,
  link: "/campaigns",
};

function getSpecialtyIcon(specialty: string): string {
  const map: Record<string, string> = {
    Electronics: "cpu",
    Firmware: "code",
    "Product Design": "cube",
    Education: "graduation-cap",
    Robotics: "robot",
    Audio: "speaker-high",
  };
  return map[specialty] ?? "wrench";
}

/** Activity feed entries */
const activities = [
  {
    icon: "rocket-launch",
    text: `Launched <strong>${showcase.name}</strong> campaign`,
    time: "3 days ago",
    highlight: true,
    miniCard: {
      icon: showcase.icon,
      name: showcase.name,
      percent: showcase.percent,
      backers: showcase.backers,
    },
  },
  {
    icon: "article",
    text: `Published build journal: <strong>Field Testing v${maker.projects}</strong>`,
    time: "1 week ago",
    highlight: false,
    miniCard: null as null | { icon: string; name: string; percent: number; backers: number },
  },
  {
    icon: "file-text",
    text: `Contributed specs to a <span class="text-stamp font-semibold">Community Request</span>`,
    time: "2 weeks ago",
    highlight: false,
    miniCard: null as null | { icon: string; name: string; percent: number; backers: number },
  },
  {
    icon: "heart",
    text: "Backed <strong>Desktop CNC Mill</strong> by Vikram Rao",
    time: "3 weeks ago",
    highlight: false,
    miniCard: null as null | { icon: string; name: string; percent: number; backers: number },
  },
  {
    icon: "trophy",
    text: `Earned badge: <strong>${maker.backers >= 100 ? "100+ Backers" : "First Backer"}</strong>`,
    time: "1 month ago",
    highlight: true,
    miniCard: null as null | { icon: string; name: string; percent: number; backers: number },
  },
  {
    icon: "user-plus",
    text: "Joined <strong>Absurd Industries</strong>",
    time: "2024",
    highlight: false,
    miniCard: null as null | { icon: string; name: string; percent: number; backers: number },
  },
];

/** Projects list */
const projects = [
  {
    name: showcase.name,
    description: showcase.description,
    icon: showcase.icon,
    status: "Live" as const,
    percent: showcase.percent,
    backers: showcase.backers,
  },
  ...(maker.projects > 1
    ? [
        {
          name: `${maker.skills[0]} Monitor`,
          description: `Open-source ${maker.skills[0].toLowerCase()} monitoring system with wireless alerts.`,
          icon: "chart-line",
          status: "Completed" as const,
          percent: 100,
          backers: Math.round(maker.backers * 0.4),
        },
      ]
    : []),
  ...(maker.projects > 2
    ? [
        {
          name: `${maker.skills[1] ?? maker.specialty} Kit`,
          description: `Upcoming kit for ${maker.specialty.toLowerCase()} enthusiasts.`,
          icon: "package",
          status: "Coming Soon" as const,
          percent: null as number | null,
          backers: 0,
        },
      ]
    : []),
];

/** Badges */
const badges = [
  {
    name: "Guild Member",
    icon: "seal-check",
    description: "Verified Absurd Industries maker",
    bg: "rgba(42,95,65,0.12)",
    color: "#2A5F41",
  },
  {
    name: "First Campaign",
    icon: "flag-banner",
    description: "Successfully funded their first campaign",
    bg: "rgba(255,89,0,0.1)",
    color: "#FF5900",
  },
  ...(maker.backers >= 100
    ? [
        {
          name: "100+ Backers",
          icon: "users-three",
          description: "Reached 100 cumulative backers",
          bg: "rgba(26,26,26,0.08)",
          color: "#1A1A1A",
        },
      ]
    : []),
  {
    name: "Community Helper",
    icon: "handshake",
    description: "Contributed to community requests",
    bg: "rgba(42,95,65,0.12)",
    color: "#2A5F41",
  },
  {
    name: `${maker.specialty} Specialist`,
    icon: "lightning",
    description: `Specialist in ${maker.specialty}`,
    bg: "rgba(107,91,74,0.12)",
    color: "#6B5B4A",
  },
  {
    name: "Open Source",
    icon: "lock-key-open",
    description: "All projects use open-source licenses",
    bg: "rgba(255,89,0,0.1)",
    color: "#FF5900",
  },
];

/** Workshop / Setup */
const workshop = {
  name: `${firstName}'s Workshop`,
  location: maker.location,
  size: "Home workshop",
  hours: "Open for visits by appointment",
  equipment: [
    { name: "3D Printer", icon: "cube" },
    { name: "Soldering Station", icon: "fire" },
    { name: "Oscilloscope", icon: "wave-sine" },
    { name: "Multimeter", icon: "lightning" },
    { name: "Power Supply", icon: "battery-charging" },
    { name: "Hot Air Station", icon: "wind" },
  ],
};

/** Comments wall */
const comments = [
  {
    id: 1,
    author: "Vikram Rao",
    time: "2 days ago",
    text: `${firstName}'s documentation is incredible. This is what open source should look like.`,
  },
  {
    id: 2,
    author: "Arjun Nair",
    time: "1 week ago",
    text: `Thanks for the detailed specs! Your ${maker.skills[0]} recommendations were exactly what we needed.`,
  },
  {
    id: 3,
    author: "Community Member",
    time: "3 weeks ago",
    text: `Exceptional work on the ${maker.specialty.toLowerCase()} projects. Keep shipping!`,
  },
];

/** External links with icons */
const externalLinks = [
  ...(maker.links?.github
    ? [{ label: maker.links.github.replace("https://", ""), url: maker.links.github, iconClass: "fa-brands fa-github" }]
    : []),
  ...(maker.links?.website
    ? [{ label: maker.links.website.replace("https://", ""), url: maker.links.website, iconClass: "ph-bold ph-globe" }]
    : []),
  ...(maker.links?.twitter
    ? [{ label: maker.links.twitter.replace("https://twitter.com/", "@"), url: maker.links.twitter, iconClass: "fa-brands fa-x-twitter" }]
    : []),
];

/** Primary tools (derived from skills for display) */
const tools = maker.skills.slice(0, 4);

/** Contribution count (mock) */
const contributions = Math.round(maker.projects * 8 + maker.backers / 10);

---

<BaseLayout
  title={`${maker.name} — ${maker.title}`}
  description={maker.bio ?? `${maker.name} is a ${maker.title} from ${maker.location} on Absurd Industries.`}
  activeTab="makers"
>
  <section class="pt-6 pb-0 sm:pt-8">
    <div class="max-w-5xl mx-auto px-5 sm:px-6">
      <a href="/makers" class="back-btn">
        <i class="ph-bold ph-arrow-left"></i>
        Makers
      </a>
    </div>
  </section>

  <!-- ══════════════════════════════════════ -->
  <!-- PROFILE BANNER                        -->
  <!-- ══════════════════════════════════════ -->
  <section class="pt-0 pb-0">
    <div class="max-w-5xl mx-auto px-5 sm:px-6">
      <div
        class="profile-banner"
        style={`--banner-bg: ${bannerColor}; --banner-accent: ${bannerAccent};`}
        data-configurable="banner"
      >
        <!-- Punch hole (card stock ring) -->
        <div class="punch-hole"></div>

        <div class="relative z-10 px-6 py-8 sm:px-10 sm:py-10">
          <div class="banner-layout">

            <!-- Hexagon Avatar -->
            <div class="relative flex-shrink-0 -mb-12 sm:-mb-14">
              <div class="hex-avatar">
                {maker.avatar ? (
                  <img src={maker.avatar} alt={maker.name} class="hex-avatar-img" />
                ) : (
                  <span class="font-serif font-bold text-4xl sm:text-5xl text-ink-faint/40 select-none">{initial}</span>
                )}
              </div>
              <div class="status-online"></div>
            </div>

            <!-- Name + rank -->
            <div class="banner-name-section">
              <div class="flex flex-wrap items-center gap-2 sm:gap-3">
                <h1 class="font-serif font-bold text-xl sm:text-2xl md:text-3xl text-paper tracking-tight leading-tight">{maker.name}</h1>
                <div class="level-badge" title={`Level ${level}`}>{level}</div>
                <span class="tag" style="background:rgba(42,95,65,0.25); color:#7FE0A0; font-size:0.6rem;">
                  <i class="ph-bold ph-seal-check mr-1" style="font-size:0.55rem;"></i>
                  Verified
                </span>
              </div>
              <p class="text-kraft text-sm mt-1 truncate">{maker.title}</p>
              <div class="flex flex-wrap items-center gap-3 mt-2 text-xs text-kraft-light">
                <span class="flex items-center gap-1">
                  <span class="w-2 h-2 rounded-full bg-funded inline-block"></span>
                  Online now
                </span>
                <span class="opacity-50 hidden sm:inline">|</span>
                <span class="hidden sm:inline">Last active 2 hours ago</span>
              </div>
            </div>

            <!-- Action buttons -->
            <div class="banner-actions">
              <a href="https://discord.gg/DUSUtguG2H" target="_blank" rel="noopener"
                class="btn btn-sm" style="border-color: rgba(250,243,232,0.3); color: #FAF3E8; background: transparent; border: 1.5px solid rgba(250,243,232,0.3);"
              >
                <i class="ph-bold ph-plus mr-1.5" style="font-size:0.75rem;"></i>
                Follow
              </a>
              <a href="https://discord.gg/DUSUtguG2H" target="_blank" rel="noopener"
                class="btn btn-sm" style="background:rgba(250,243,232,0.12); color:#FAF3E8; border:1.5px solid rgba(250,243,232,0.2);"
              >
                <i class="ph-bold ph-envelope-simple mr-1.5" style="font-size:0.75rem;"></i>
                Message
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════ -->
  <!-- TWO-COLUMN LAYOUT                     -->
  <!-- ══════════════════════════════════════ -->
  <section class="pt-16 sm:pt-18 pb-14 md:pb-20">
    <div class="max-w-5xl mx-auto px-5 sm:px-6">
      <div class="flex flex-col lg:flex-row gap-6">

        <!-- ══════════════════════════ -->
        <!-- MAIN COLUMN (LEFT)         -->
        <!-- ══════════════════════════ -->
        <div class="flex-1 min-w-0 space-y-6">

          <!-- ── SHOWCASE (Pinned) ── -->
          <div data-configurable="showcase">
            <div class="section-label">
              <span class="text-xs font-semibold uppercase tracking-wider text-ink-faint flex items-center gap-1.5">
                <i class="ph-bold ph-push-pin text-stamp" style="font-size:0.7rem;"></i>
                Featured Showcase
              </span>
            </div>
            <div class="showcase-border">
              <div class="card" style="border-radius:0.8rem; overflow:hidden;">
                <div class="card-bg" style="border-radius:0.8rem;"></div>
                <div class="card-content">
                  <div class="flex flex-col sm:flex-row">
                    <!-- Showcase image area -->
                    <a href={showcase.link} class="block sm:w-56 flex-shrink-0">
                      <div class="h-40 sm:h-full bg-kraft-light flex items-center justify-center" style="border-radius:0.8rem 0 0 0; min-height:160px;">
                        <i class={`ph-bold ph-${showcase.icon} text-6xl text-stencil opacity-40`}></i>
                      </div>
                    </a>
                    <div class="p-5 sm:p-6 flex-1">
                      <div class="flex flex-wrap items-center gap-2 mb-2">
                        <Tag variant="funded">
                          <span class="w-1.5 h-1.5 rounded-full bg-funded mr-1 pulse-soft inline-block"></span>
                          Live
                        </Tag>
                        <Tag>{showcase.category}</Tag>
                        <Tag><i class="ph-bold ph-scales mr-1"></i>{showcase.license}</Tag>
                      </div>
                      <a href={showcase.link} class="no-underline text-ink">
                        <h3 class="font-serif font-bold text-lg sm:text-xl leading-snug mb-1.5 hover:text-stamp transition-colors">{showcase.name}</h3>
                      </a>
                      <p class="text-sm text-ink-faint leading-relaxed mb-4">{showcase.description}</p>

                      <ProgressBar percent={showcase.percent} />
                      <div class="flex justify-between text-xs text-ink-faint mt-1.5">
                        <span><strong class="text-ink font-semibold">{showcase.percent}%</strong> of {showcase.goal}</span>
                        <span>{showcase.backers} backers</span>
                      </div>

                      <a href={showcase.link} class="btn btn-stamp btn-sm mt-4">
                        View Campaign <i class="ph-bold ph-arrow-right ml-1.5" style="font-size:0.7rem;"></i>
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- ── ACTIVITY FEED ── -->
          <Card>
            <div class="p-6 sm:p-8">
              <div class="flex items-center justify-between mb-5">
                <h2 class="font-serif font-bold text-xl sm:text-2xl tracking-tight">Recent Activity</h2>
              </div>

              <div class="relative pl-8">
                <div class="timeline-line"></div>
                {activities.map((a) => (
                  <div class="relative flex items-start gap-3 pb-5 last:pb-0">
                    <div
                      class:list={["timeline-dot absolute", { highlight: a.highlight }]}
                      style="left: -27px; top: 4px;"
                    ></div>
                    <div class="flex-1">
                      <div class="flex items-start gap-2">
                        <i class={`ph-bold ph-${a.icon} text-sm mt-0.5`} style={`color: ${a.highlight ? "#FF5900" : "#6B5B4A"}`}></i>
                        <div class="flex-1">
                          <p class="text-sm text-ink leading-snug" set:html={a.text} />
                          <span class="text-xs text-ink-faint mt-0.5 block">{a.time}</span>

                          {/* Inline mini card for campaign activity */}
                          {a.miniCard && (
                            <div class="mt-2 p-3 rounded-lg bg-kraft-light/50 border border-ink/5">
                              <div class="flex items-center gap-2 mb-1.5">
                                <i class={`ph-bold ph-${a.miniCard.icon} text-sm text-stencil`}></i>
                                <span class="text-xs font-semibold text-ink">{a.miniCard.name}</span>
                              </div>
                              <div class="progress mb-1" style="height:4px;">
                                <div class="progress-fill" style={`width: ${a.miniCard.percent}%`}></div>
                              </div>
                              <div class="text-[0.6rem] text-ink-faint">
                                <strong class="text-ink font-semibold">{a.miniCard.percent}%</strong> funded &middot; {a.miniCard.backers} backers
                              </div>
                            </div>
                          )}
                        </div>
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </Card>

          <!-- ── PROJECTS GRID ── -->
          <Card>
            <div class="p-6 sm:p-8">
              <div class="flex items-center justify-between mb-5">
                <h2 class="font-serif font-bold text-xl sm:text-2xl tracking-tight">All Projects</h2>
                <span class="text-xs text-ink-faint font-semibold">{projects.length} projects</span>
              </div>

              <div class="space-y-3">
                {projects.map((project) => (
                  <a
                    href="/campaigns"
                    class="flex items-center gap-4 p-3 rounded-lg hover:bg-kraft-light/40 transition-colors no-underline text-ink group"
                  >
                    {/* Project icon */}
                    <div class="w-12 h-12 rounded-lg bg-kraft-light flex items-center justify-center flex-shrink-0">
                      <i class={`ph-bold ph-${project.icon} text-xl text-stencil group-hover:text-stamp transition-colors`}></i>
                    </div>

                    {/* Project info */}
                    <div class="flex-1 min-w-0">
                      <div class="flex items-center gap-2 mb-0.5">
                        <h3 class="text-sm font-semibold truncate group-hover:text-stamp transition-colors">{project.name}</h3>
                        <Tag
                          variant={project.status === "Live" ? "funded" : project.status === "Completed" ? "stamp" : "default"}
                          class="text-[0.55rem] flex-shrink-0"
                        >
                          {project.status}
                        </Tag>
                      </div>
                      <p class="text-xs text-ink-faint truncate">{project.description}</p>
                    </div>

                    {/* Progress or status */}
                    <div class="w-24 flex-shrink-0 hidden sm:block">
                      {project.percent !== null ? (
                        <div>
                          <ProgressBar percent={project.percent} hot={project.percent >= 100} />
                          <div class="text-[0.6rem] text-ink-faint text-right mt-1">{project.percent}%</div>
                        </div>
                      ) : (
                        <div class="text-xs text-ink-faint italic text-right">Upcoming</div>
                      )}
                    </div>

                    <i class="ph-bold ph-caret-right text-xs text-ink-faint/40 flex-shrink-0"></i>
                  </a>
                ))}
              </div>
            </div>
          </Card>

          <!-- ── WORKSHOP / SETUP ── -->
          <Card variant="dark">
            <div class="p-6 sm:p-8 text-paper">
              <div class="flex items-center gap-2 mb-5">
                <i class="ph-bold ph-wrench text-stamp text-xl"></i>
                <h2 class="font-serif font-bold text-xl sm:text-2xl tracking-tight">Workshop / Setup</h2>
              </div>

              <div class="flex flex-col sm:flex-row gap-6">
                <!-- Workshop info -->
                <div class="flex-1">
                  <h3 class="font-serif font-semibold text-base text-paper mb-2">{workshop.name}</h3>
                  <div class="space-y-2 mb-4">
                    <div class="flex items-center gap-2 text-sm text-kraft">
                      <i class="ph-bold ph-map-pin text-xs text-stamp"></i>
                      {workshop.location}
                    </div>
                    <div class="flex items-center gap-2 text-sm text-kraft">
                      <i class="ph-bold ph-ruler text-xs text-kraft-dark"></i>
                      {workshop.size}
                    </div>
                    <div class="flex items-center gap-2 text-sm text-kraft">
                      <i class="ph-bold ph-clock text-xs text-kraft-dark"></i>
                      {workshop.hours}
                    </div>
                  </div>

                  <a href="/inventory" class="flex items-center gap-2 text-sm font-semibold text-stamp hover:underline no-underline">
                    <i class="ph-bold ph-package text-sm"></i>
                    View full lab inventory
                    <i class="ph-bold ph-arrow-right text-xs"></i>
                  </a>
                </div>

                <!-- Equipment grid -->
                <div class="sm:w-56 flex-shrink-0">
                  <div class="text-xs font-semibold uppercase tracking-wider text-kraft-dark mb-3">Equipment</div>
                  <div class="grid grid-cols-2 gap-2">
                    {workshop.equipment.map((e) => (
                      <div class="flex items-center gap-2 p-2 rounded-lg" style="background:rgba(250,243,232,0.06);">
                        <div class="equip-icon" style="width:28px; height:28px; font-size:0.85rem;">
                          <i class={`ph-bold ph-${e.icon}`}></i>
                        </div>
                        <span class="text-[0.65rem] text-kraft leading-tight">{e.name}</span>
                      </div>
                    ))}
                  </div>
                </div>
              </div>

              <!-- Workspace photo placeholders -->
              <div class="mt-5 pt-5 border-t border-paper/10">
                <div class="text-xs font-semibold uppercase tracking-wider text-kraft-dark mb-3">Workspace Photos</div>
                <div class="grid grid-cols-3 gap-2">
                  {[1, 2, 3].map(() => (
                    <div class="aspect-video rounded-lg flex items-center justify-center" style="background:rgba(250,243,232,0.08);">
                      <i class="ph-bold ph-image text-xl text-paper/20"></i>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </Card>

          <!-- ── COMMENTS / WALL ── -->
          <Card>
            <div class="p-6 sm:p-8">
              <div class="flex items-center justify-between mb-5">
                <h2 class="font-serif font-bold text-xl sm:text-2xl tracking-tight">Profile Wall</h2>
                <span class="text-xs text-ink-faint font-semibold">{comments.length} comments</span>
              </div>

              <!-- Existing comments -->
              <div class="mb-5">
                {comments.map((c) => (
                  <div class="comment-entry">
                    <div class="flex items-start gap-3">
                      <div class="w-8 h-8 rounded-full bg-kraft-light flex items-center justify-center flex-shrink-0 mt-0.5">
                        <i class="ph-bold ph-user text-xs text-ink-faint"></i>
                      </div>
                      <div class="flex-1">
                        <div class="flex items-center gap-2 mb-1">
                          <span class="text-sm font-semibold text-ink">{c.author}</span>
                          <span class="text-[0.6rem] text-ink-faint">{c.time}</span>
                        </div>
                        <p class="text-sm text-ink-light leading-relaxed">{c.text}</p>
                      </div>
                    </div>
                  </div>
                ))}
              </div>

              <!-- Comment form placeholder -->
              <div class="border-t border-ink/5 pt-4">
                <div class="flex items-start gap-3">
                  <div class="w-8 h-8 rounded-full bg-kraft flex items-center justify-center flex-shrink-0 mt-0.5">
                    <i class="ph-bold ph-user text-xs text-ink-faint"></i>
                  </div>
                  <div class="flex-1">
                    <textarea
                      rows="2"
                      placeholder={`Leave a comment on ${firstName}'s wall...`}
                      class="w-full p-3 rounded-lg border border-ink/10 bg-paper text-sm focus:outline-none focus:border-stamp focus:ring-1 focus:ring-stamp/30 resize-none"
                      disabled
                    ></textarea>
                    <div class="flex justify-end mt-2">
                      <span class="btn btn-primary btn-sm" style="opacity:0.4; cursor:not-allowed; transform:none;">
                        <i class="ph-bold ph-paper-plane-tilt mr-1.5" style="font-size:0.7rem;"></i>
                        Post Comment
                      </span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </Card>

        </div>

        <!-- ══════════════════════════ -->
        <!-- SIDEBAR (RIGHT)            -->
        <!-- ══════════════════════════ -->
        <div class="w-full lg:w-80 flex-shrink-0 space-y-5">

          <!-- ── QUICK INFO ── -->
          <Card>
            <div class="p-5 sm:p-6">
              <h3 class="font-serif font-bold text-base mb-4">Profile Info</h3>

              <div class="space-y-3 text-sm mb-5">
                <div class="flex items-center justify-between">
                  <span class="text-ink-faint flex items-center gap-2">
                    <i class="ph-bold ph-map-pin text-xs"></i> Location
                  </span>
                  <span class="font-medium">{maker.location}</span>
                </div>
                <div class="flex items-center justify-between">
                  <span class="text-ink-faint flex items-center gap-2">
                    <i class="ph-bold ph-calendar-blank text-xs"></i> Member since
                  </span>
                  <span class="font-medium">2024</span>
                </div>
                <div class="flex items-center justify-between">
                  <span class="text-ink-faint flex items-center gap-2">
                    <i class="ph-bold ph-lightning text-xs"></i> Specialty
                  </span>
                  <span class="font-medium">{maker.specialty}</span>
                </div>
              </div>

              <!-- Currently working on -->
              <div class="p-3 rounded-lg bg-kraft-light/50 border border-ink/5 mb-5">
                <div class="text-[0.6rem] font-semibold uppercase tracking-wider text-ink-faint mb-1.5">Currently working on</div>
                <p class="text-sm text-ink leading-snug font-medium">{showcase.name}</p>
              </div>

              <!-- Stats row -->
              <div class="grid grid-cols-3 gap-3 text-center mb-5">
                <div>
                  <div class="font-serif font-bold text-lg text-ink">{maker.projects}</div>
                  <div class="text-[0.6rem] text-ink-faint font-semibold uppercase tracking-wide">Projects</div>
                </div>
                <div>
                  <div class="font-serif font-bold text-lg text-ink">{maker.backers}</div>
                  <div class="text-[0.6rem] text-ink-faint font-semibold uppercase tracking-wide">Backers</div>
                </div>
                <div>
                  <div class="font-serif font-bold text-lg text-ink">{contributions}</div>
                  <div class="text-[0.6rem] text-ink-faint font-semibold uppercase tracking-wide">Contribs</div>
                </div>
              </div>

              <!-- Links -->
              {externalLinks.length > 0 && (
                <div class="border-t border-ink/5 pt-4 space-y-2.5">
                  {externalLinks.map((link) => (
                    <a href={link.url} target="_blank" rel="noopener" class="flex items-center gap-2.5 text-sm text-ink hover:text-stamp transition-colors no-underline group">
                      <i class={`${link.iconClass} text-base text-ink-faint group-hover:text-stamp transition-colors`}></i>
                      <span class="font-medium">{link.label}</span>
                      <i class="ph-bold ph-arrow-square-out text-xs text-ink-faint/50 ml-auto"></i>
                    </a>
                  ))}
                </div>
              )}
            </div>
          </Card>

          <!-- ── BADGES / ACHIEVEMENTS ── -->
          <Card>
            <div class="p-5 sm:p-6" data-configurable="badges">
              <div class="flex items-center justify-between mb-4">
                <h3 class="font-serif font-bold text-base">Badges</h3>
                <span class="text-[0.6rem] text-ink-faint font-semibold uppercase tracking-wide">{badges.length} earned</span>
              </div>

              <div class="grid grid-cols-3 gap-1">
                {badges.map((b) => (
                  <div class="badge-item" title={b.description}>
                    <div class="badge-icon" style={`background: ${b.bg}; color: ${b.color};`}>
                      <i class={`ph-bold ph-${b.icon}`}></i>
                    </div>
                    <span class="text-[0.58rem] text-ink-faint text-center font-semibold leading-tight">{b.name}</span>
                  </div>
                ))}
              </div>
            </div>
          </Card>

          <!-- ── SKILLS & TOOLS ── -->
          <Card>
            <div class="p-5 sm:p-6">
              <h3 class="font-serif font-bold text-base mb-4">Skills & Tools</h3>

              <div class="mb-4">
                <div class="text-xs font-semibold uppercase tracking-wider text-ink-faint mb-2">Skills</div>
                <div class="flex flex-wrap gap-1.5">
                  {maker.skills.map((s) => (
                    <Tag class="text-[0.62rem]">{s}</Tag>
                  ))}
                </div>
              </div>

              <div>
                <div class="text-xs font-semibold uppercase tracking-wider text-ink-faint mb-2">Primary Tools</div>
                <div class="flex flex-wrap gap-1.5">
                  {tools.map((t) => (
                    <Tag class="text-[0.62rem]">{t}</Tag>
                  ))}
                </div>
              </div>
            </div>
          </Card>

          <!-- ── BIO ── -->
          <Card>
            <div class="p-5 sm:p-6">
              <h3 class="font-serif font-bold text-base mb-3">About</h3>
              <p class="text-sm text-ink-light leading-relaxed">{maker.bio ?? `${maker.name} is a ${maker.title} based in ${maker.location}.`}</p>
            </div>
          </Card>

        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* ================================================================ */
  /* STEAM-STYLE PROFILE — page-scoped styles                        */
  /* ================================================================ */

  /* Breadcrumb */
  .breadcrumb a {
    text-decoration: none;
    font-weight: 500;
  }

  /* Profile banner — filter on pseudo-element to avoid blurring content */
  .profile-banner {
    --banner-bg: #1A1A1A;
    --banner-accent: #FF5900;
    --banner-pattern: repeating-linear-gradient(
      135deg,
      transparent,
      transparent 20px,
      rgba(255, 89, 0, 0.04) 20px,
      rgba(255, 89, 0, 0.04) 22px
    );
    position: relative;
    border-radius: 0.875rem;
    overflow: hidden;
  }
  .profile-banner::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--banner-bg);
    background-image: var(--banner-pattern);
    filter: url(#papercut);
    z-index: 0;
    pointer-events: none;
  }

  /* Punch hole — card stock ring that cuts through the banner */
  .punch-hole {
    position: absolute;
    top: 14px;
    left: 50%;
    transform: translateX(-50%);
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: var(--banner-bg);
    box-shadow:
      inset 0 1px 3px rgba(0,0,0,0.4),
      inset 0 -1px 2px rgba(255,255,255,0.05),
      0 0 0 3px rgba(255,255,255,0.08);
    z-index: 20;
  }
  .punch-hole::after {
    content: '';
    position: absolute;
    inset: 4px;
    border-radius: 50%;
    background: #D4B896;
    box-shadow: inset 0 1px 2px rgba(0,0,0,0.15);
  }

  /* Banner layout — prevents smushing on narrow screens */
  .banner-layout {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  @media (min-width: 640px) {
    .banner-layout {
      flex-direction: row;
      align-items: flex-end;
      gap: 1.25rem;
    }
  }
  .banner-name-section {
    flex: 1;
    min-width: 0;
    padding-bottom: 2px;
  }
  .banner-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }
  @media (min-width: 640px) {
    .banner-actions {
      padding-bottom: 2px;
    }
  }

  /* Hexagon avatar */
  .hex-avatar {
    width: 96px;
    height: 96px;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
    background: #FAF3E8;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
    overflow: hidden;
  }
  .hex-avatar-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  @media (min-width: 640px) {
    .hex-avatar {
      width: 112px;
      height: 112px;
    }
  }

  /* Online status dot */
  .status-online {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #2A5F41;
    border: 2px solid #1A1A1A;
    position: absolute;
    bottom: 2px;
    right: 2px;
  }
  @keyframes blink-online {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
  .status-online {
    animation: blink-online 3s ease-in-out infinite;
  }

  /* Level badge */
  .level-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 0.5rem;
    background: linear-gradient(135deg, #FF5900, #CC4700);
    color: #FAF3E8;
    font-family: 'Fraunces', Georgia, serif;
    font-weight: 800;
    font-size: 0.875rem;
    box-shadow: 0 2px 8px rgba(255, 89, 0, 0.3);
  }

  /* Showcase section */
  .showcase-border {
    background: linear-gradient(135deg, rgba(255, 89, 0, 0.15), rgba(42, 95, 65, 0.1));
    border: 1.5px solid rgba(255, 89, 0, 0.2);
    border-radius: 0.875rem;
    padding: 1px;
  }

  /* Badge grid */
  .badge-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.35rem;
    padding: 0.5rem 0.25rem;
    border-radius: 0.5rem;
    transition: background 0.15s;
    cursor: default;
  }
  .badge-item:hover {
    background: rgba(26, 26, 26, 0.04);
  }
  .badge-icon {
    width: 40px;
    height: 40px;
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;
  }

  /* Timeline */
  .timeline-line {
    position: absolute;
    left: 15px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: rgba(26, 26, 26, 0.08);
  }
  .timeline-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #D4B896;
    border: 2px solid #B8956A;
    position: relative;
    z-index: 1;
    flex-shrink: 0;
  }
  .timeline-dot.highlight {
    background: #FF5900;
    border-color: #CC4700;
  }

  /* Equipment icons */
  .equip-icon {
    width: 40px;
    height: 40px;
    border-radius: 0.5rem;
    background: rgba(250, 243, 232, 0.12);
    display: flex;
    align-items: center;
    justify-content: center;
    color: rgba(250, 243, 232, 0.6);
    font-size: 1.1rem;
  }

  /* Comment wall */
  .comment-entry {
    padding-bottom: 0.75rem;
    margin-bottom: 0.75rem;
    border-bottom: 1px solid rgba(26, 26, 26, 0.06);
  }
  .comment-entry:last-child {
    border-bottom: none;
    padding-bottom: 0;
    margin-bottom: 0;
  }

  /* Section divider label */
  .section-label {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
  }
  .section-label::after {
    content: '';
    flex: 1;
    height: 1px;
    background: rgba(26, 26, 26, 0.1);
  }
</style>
